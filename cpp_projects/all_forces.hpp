#include <iostream>
#include "PhysObj.hpp"
#include <cmath>

int all_forces(PhysObj new_obj){
    PhysObj::PhysObj gravity(const G = 9.8){
        new_obj.force(M_PI, G);
        return new_obj;
    }
}
